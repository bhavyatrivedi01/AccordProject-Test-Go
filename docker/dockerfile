FROM golang:1.25-alpine

RUN apk add --no-cache \
    nodejs \
    npm \
    libstdc++ \
    g++ \
    make \
    bash \
    git

RUN npm install -g @accordproject/concerto-cli@3.18.2 --ignore-scripts

WORKDIR /workspace

ENTRYPOINT ["concerto"]
